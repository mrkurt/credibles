<%= raw ((@edit.email.blank? && '(Anonymous)') || @edit.email) %> just submitted a correction for one of your pages:
<%= raw @edit.url + '#copypasta-auto' %>

<% @edit.suggestions.each do |s| %>
Proposed:
<%= raw diff_text(diff_after(s, false)) %>

Original:
<%= raw diff_text(diff_before(s, false)) %>
<% end %>

Comments from user:
<%= raw @edit.comments %>

<% unless @edit.page.account.edit_url_format.blank? %>
Edit link:
<%= raw @edit.page.account.edit_url_format.gsub('{{key}}', @edit.page.key) %>
<% end %>
